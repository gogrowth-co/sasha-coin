Create a branded presentation deck using the `branded-deck` skill.

First, invoke the `branded-deck` skill to load the design system, layout catalog, and PptxGenJS workflow.

Then read brand context from `_context/brand-voice.md` and `_context/product-info.md`.

The user wants: $ARGUMENTS

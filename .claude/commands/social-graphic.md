Create a branded social media graphic or carousel using the `social-graphics` skill.

First, invoke the `social-graphics` skill to load the nanobanana workflow, platform specs, and format catalog.

Then read the visual system from `_templates/social-creatives/linkedin-image-style-guide.md` and brand context from `_context/style-guide.md`.

The user wants: $ARGUMENTS
